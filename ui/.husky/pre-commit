set -e

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root/ui"

# Type generation requires a running authenticated API, so it belongs in an
# explicit generation workflow rather than every commit.
echo "Running the UI lint gate..."
pnpm lint
