set -e

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

if ! python -m pre_commit --version >/dev/null 2>&1; then
  echo "pre-commit is required. Install the project dev dependencies first." >&2
  exit 1
fi

echo "Running staged agent skill routing and repository checks..."
exec python -m pre_commit run
