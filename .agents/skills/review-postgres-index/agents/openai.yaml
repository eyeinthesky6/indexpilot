interface:
  display_name: "Review PostgreSQL Index"
  short_description: "Install IndexPilot and test a proposed index safely"
  default_prompt: "Use $review-postgres-index to install IndexPilot, run its safe first review, and explain the outcome and limits."
