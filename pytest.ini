[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
# Note: Global timeout causes pytest collection to hang
# Use @pytest.mark.timeout() decorator on individual slow tests instead

